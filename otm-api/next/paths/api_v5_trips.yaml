put:
  operationId: createTrip
  responses:
    '200':
      description: The same entity
      content:
        application/json:
          schema:
            $ref: ../components/schemas/entity/trip.yaml
    '400':
      description: Client error
      content:
        application/json:
          schema:
            $ref: ../components/schemas/error/clientError.yaml
    '500':
      description: Server error
      content:
        application/json:
          schema:
            $ref: ../components/schemas/error/serverError.yaml
  summary: Adds a new Trip
  requestBody:
    content:
      application/json:
        schema:
          $ref: ../components/schemas/entity/trip.yaml
  tags:
    - Trip
  security:
    - BearerToken: []
