type: object
properties:
  associationType:
    example: inline
    type: string
    enum:
      - inline
  entity:
    $ref: ./document-owner.yaml
  description:
    description: A free text description of the relationship to the associated entity.
    type: string
  roles:
    $ref: ./actor-roles.yaml
required:
  - associationType
  - entity
