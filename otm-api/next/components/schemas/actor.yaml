type: object
oneOf:
  - $ref: ./actor-base.yaml
  - $ref: ./actor-person.yaml
  - $ref: ./actor-company.yaml
discriminator:
  propertyName: type
  mapping:
    person: ./actor-person.yaml
    company: ./actor-company.yaml
    # Note: we intentionally do not map 'actor-base' because using the base-only may omit the 'type' property
