type: object
properties:
  type:
    example: phone
    type: string
    enum:
      - phone
  value:
    description: >-
      A phone number, note that this means the land line phone number. Mobile
      phone is a separate type of contact detail
    type: string
  remark:
    description: Remark for this phone.
    type: string
  language:
    description: Language required for contacting. One must use the ISO 639-3 codes.
    type: string
required:
  - type
  - value
