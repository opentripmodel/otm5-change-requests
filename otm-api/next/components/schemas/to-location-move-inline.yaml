type: object
properties:
  associationType:
    example: inline
    type: string
    enum:
      - inline
  entity:
    $ref: ./to-location-move.yaml
  description:
    description: A free text description of the relationship to the associated entity.
    type: string
required:
  - associationType
  - entity
